services:
  discord-bot:
    build: .
    container_name: discord_audio_bot
    restart: always
    privileged: true
    network_mode: host
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - COMMAND_BASE=${COMMAND_BASE:-radio}
      - RECOVERY_MODE=${RECOVERY_MODE:-resume}
    volumes:
      - discord_bot_data:/data
      - ./sources:/sources:ro
    devices:
      - "/dev/bus/usb:/dev/bus/usb"
      - "/dev/snd:/dev/snd"

volumes:
  discord_bot_data:
    name: discord_bot_data
